void setup(){
size(200,200);
background(255);
};

void draw(){ //nothing happens here

};

void mousePressed(){ //whenever i press the mouse key it executes the code

stroke(0);
fill(175);
rectMode(CENTER);
rect(mouseX,mouseY,16,16);
};

void keyPressed(){  /*whenever i press any key it executes the code (make a white 
background(255);          background , like a reset button*/
};
